{
  "name": "kabirs-eleven",
  "version": "1.0.0",
  "private": true,
  "description": "Cricket player management app",
  "scripts": {
    "test": "node --test",
    "test:coverage": "c8 --reporter=text-summary --reporter=html --reporter=lcov --reporter=json-summary --check-coverage --lines 90 --functions 90 --branches 85 node --test"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "jsdom": "^22.1.0"
  }
}
